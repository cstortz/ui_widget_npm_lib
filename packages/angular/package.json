{
  "name": "@cstortz/widget-system-angular",
  "version": "0.0.1",
  "description": "Angular 17+ components and services for @cstortz/widget-system",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "echo 'Angular tests — add ng test'",
    "lint": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "@angular/core": ">=17",
    "@cstortz/widget-system": "*"
  },
  "dependencies": {
    "@cstortz/widget-system": "*"
  },
  "keywords": ["angular", "widget", "workspace"],
  "license": "AGPL-3.0-or-later",
  "publishConfig": {
    "access": "public"
  }
}
