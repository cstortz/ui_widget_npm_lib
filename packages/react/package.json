{
  "name": "@cstortz/widget-system-react",
  "version": "0.0.1",
  "description": "React 18+ components and hooks for @cstortz/widget-system",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "echo 'React tests — add vitest'",
    "lint": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "react": ">=18",
    "@cstortz/widget-system": "*"
  },
  "dependencies": {
    "@cstortz/widget-system": "*"
  },
  "keywords": ["react", "widget", "workspace"],
  "license": "AGPL-3.0-or-later",
  "publishConfig": {
    "access": "public"
  }
}
